;; SCM includes Hobbit, the Scheme-to-C compiler originally written by
;; Tanel Tammet. Hobbit generates C files whose binaries can be
;; dynamically or statically linked with a SCM executable.

(title "Hobbit")

(person "Tanel Tammet")
